# Netlify build + SPA routing for the Case Study Composer front end.
[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: every route falls back to index.html so React Router can handle it.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
